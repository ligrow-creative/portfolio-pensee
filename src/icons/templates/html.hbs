<!DOCTYPE html>
<html lang="en">
<head>
  <title>{{ fontName }}</title>
  <link href="{{ fontName }}.css" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
    }
    html, body {
      overflow: hidden;
    }
    body {
      user-select: none;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen-Sans, Ubuntu, Cantarell, 'Helvetica Neue', sans-serif;;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      margin: 0;
      padding: 48px 32px;
      background: #f7f7f7;
      color: #232323;
    }
    h1 {
      margin: 0 0 32px;
      font-size: 32px;
      font-weight: bold;
      text-transform: capitalize;
    }
    blockquote {
      padding: 0;
      margin: 0;
      text-transform: uppercase;
      font-size: 13px;
      font-weight: 500;
      letter-spacing: 1px;
      color: #666;
    }
    .container {
      max-width: 848px;
      margin-inline: auto;
    }
    .icons {
      display: grid;
      grid-template-columns: repeat(auto-fill, 200px);
      gap: 16px;
      margin-top: 32px;
    }
    .icons__item {
      display: flex;
      align-items: center;
      flex-direction: column;
      background-color: #fff;
      border: 1px solid #ddd;
      padding: 18px 16px 16px;
      border-radius: 4px;
    }
    .icons__item i {
      margin-bottom: 8px;
    }
    .icons__item input {
      border: 0;
      padding: 4px 12px;
      border-radius: 0;
      outline: none;
      font-size: 16px;
      text-align: center;
      color: #999;
      letter-spacing: 0.04em;
      text-overflow: ellipsis;
    }
    .icons__item i {
      font-size: 64px;
    }
  </style>
</head>
  <body>
    <div class="container">
      <blockquote>CSS Reference</blockquote>
      <h1>{{ fontName }}</h1>
      <ul class="icons">
        {{#each unicode as |item|}}
        <li class="icons__item">
          <i class="{{ removePeriods ../baseSelector }} {{ ../classPrefix }}{{ item.[0] }}" aria-hidden></i>
          <input type="text" value="{{ ../classPrefix }}{{ item.[0] }}" readonly>
          <input type="text" value="{{ item.[1] }}" readonly>
        </li>
        {{/each}}
      </ul>
    </div>
    <script>
      (function() {var ref = document.getElementsByTagName('input'); for (var i = 0; i < ref.length; i++) { ref[i].addEventListener('click', function(e) { if (e.target.tagName === 'INPUT') { return e.target.select() }})}}).call(this)
    </script>
  </body>
</html>
